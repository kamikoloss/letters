{
  "name": "server",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "dev2": "wrangler dev --x-remote-bindings",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20250819.0",
    "hono": "^4.9.2"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}
